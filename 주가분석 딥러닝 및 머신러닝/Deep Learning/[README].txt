0. 모두 재현하려면 아래의 파일과 폴더를 받으세요
 - data preprocessing부터 하이퍼파라미터 튜닝부터 학습까지 진행됩니다.
   ipynd 파일들을 순서대로 실행합니다.(keras tuner 스킵 가능)
 - dirs
  - dataset
 - files
  - ALL .py files
  - ALL .ipynd files

1. data preprocssing을 스킵하고 학습만 진행하려면 아래의 파일과 폴더를 받으세요
  - dirs
   - pickle
  - files
   - ALL .py files
   - 3. keras tuner.ipynd(모델 하이퍼파라미터 찾기. 스킵가능)
   - 4. 1-D CNN model.ipynd

2. 학습된 모델로부터 예측 결과를 시각화 하려면 아래의 파일과 폴더를 받으세요
  - dirs
   - revised_dataset
   - models
  - files
   - ALL .py files
   - 5. Predict Model.ipynd

3. 학습된 모델의 예측 결과의 시각화를 보려면 아래의 폴더를 받으세요
  - dirs
   - scaled_model_images

